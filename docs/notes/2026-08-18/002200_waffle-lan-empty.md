# Waffle empty on http://192.168.1.55:3300/

API always had four peers; the rail showed the initial empty copy while
collect blocked on dead synth/metabase, and Vite stripped Host so LAN
rebase never ran.

Parallel 1.5s Status probes; rebase this-host URLs from X-Forwarded-Host
/ Host when the browser is a LAN IP; portal the rail out of Ant header.
