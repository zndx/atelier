"""Mass-function calibration tools.

Offline calibration sweep over per-channel discount/scaling parameters
using existing classifications.json evidence. Output operating points
that translate directly into runtime config knobs.
"""
