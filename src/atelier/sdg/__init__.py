"""SDG corpora integration — sampling, provenance, first-run substrate.

The ``external/sdg-corpora`` submodule pin is the dataset version.  This
package derives *local* artifacts from it (into the configurable
artifact root); nothing here mutates the submodule.
"""
